export type Service = {
  id: number;
  name: string;
  artist: string;
  price: string;
  image: string;
  description: string;
  category: string;
};

export type Category = {
  id: number;
  name: string;
  image: string;
};
